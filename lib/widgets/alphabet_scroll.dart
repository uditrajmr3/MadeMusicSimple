import 'package:flutter/material.dart';

class AlphabetScroll extends StatelessWidget {
  const AlphabetScroll({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
